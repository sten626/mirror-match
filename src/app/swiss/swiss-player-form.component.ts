import { Component } from '@angular/core';
import { FormGroup } from '@angular/forms';

@Component({
  selector: 'mm-swiss-player-form',
  templateUrl: 'swiss-player-form.html'
})
export class SwissPlayerFormComponent {
  swissPlayerForm: FormGroup;
}
