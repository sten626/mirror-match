import { PairingsFilterPipe } from './pairings-filter.pipe';

describe('PairingsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PairingsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
