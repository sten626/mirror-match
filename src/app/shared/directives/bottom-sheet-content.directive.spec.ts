import { BottomSheetContentDirective } from './bottom-sheet-content.directive';

describe('BottomSheetContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomSheetContentDirective();
    expect(directive).toBeTruthy();
  });
});
